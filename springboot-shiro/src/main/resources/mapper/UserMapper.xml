<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oycbest.shirodemo.mapper.UserMapper">

    <resultMap type="com.oycbest.shirodemo.domain.User" id="SsmUserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="account" column="account" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="salt" column="salt" jdbcType="VARCHAR"/>
        <result property="nickName" column="nick_name" jdbcType="VARCHAR"/>
        <result property="constellation" column="constellation" jdbcType="VARCHAR"/>
        <result property="age" column="age" jdbcType="VARCHAR"/>
        <result property="sex" column="sex" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <collection property="roles" javaType="java.util.Set" resultMap="RoleMap"/>
    </resultMap>
    <resultMap type="com.oycbest.shirodemo.domain.Role" id="RoleMap">
        <result property="roleId" column="role_id" jdbcType="INTEGER"/>
        <result property="roleName" column="role_name" jdbcType="VARCHAR"/>
        <result property="roleKey" column="role_key" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="querySql">
        SELECT
            u.id,
            u.NAME,
            u.account,
            u.`password`,
            u.`salt`,
            u.nick_name,
            u.constellation,
            u.age,
            u.sex,
            u.create_time,
            r.role_id,
            r.role_name,
            r.role_key
        FROM user u
            left join user_role ur on u.id = ur.user_id
            left join role r on ur.role_id = r.role_id
    </sql>

    <!--查询单个-->
    <select id="queryById" resultMap="SsmUserMap">
       <include refid="querySql" />
        WHERE u.id =  #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SsmUserMap">
        <include refid="querySql" />
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="SsmUserMap">
        <include refid="querySql" />
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="account != null and account != ''">
                and account = #{account}
            </if>
            <if test="nickName != null and nickName != ''">
                and nick_name = #{nickName}
            </if>
            <if test="constellation != null and constellation != ''">
                and constellation = #{constellation}
            </if>
            <if test="age != null and age != ''">
                and age = #{age}
            </if>
            <if test="sex != null and sex != ''">
                and sex = #{sex}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into user(name, account, nick_name, constellation, age, sex, create_time)
        values (#{name}, #{account}, #{nickName}, #{constellation}, #{age}, #{sex}, #{createTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="account != null and account != ''">
                account = #{account},
            </if>
            <if test="nickName != null and nickName != ''">
                nick_name = #{nickName},
            </if>
            <if test="constellation != null and constellation != ''">
                constellation = #{constellation},
            </if>
            <if test="age != null and age != ''">
                age = #{age},
            </if>
            <if test="sex != null and sex != ''">
                sex = #{sex},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>

    <select id="queryByAccount" resultMap="SsmUserMap">
        <include refid="querySql" /> where u.account = #{account} limit 1
    </select>

</mapper>
